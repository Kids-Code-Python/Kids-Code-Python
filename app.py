print('app.py: Importing packages...')
import os
from flask import *
import tests
os.system('clear')

app = Flask(__name__)
app.config['isaiah08', 'user'] = {
	'ocupation': ['teacher', 'coder'], 
	'classes': ['How to take over the world with python', 'Creating a blog with Flask'],
	'password': os.getenv('user_isaiah08' )}
db = app.config
